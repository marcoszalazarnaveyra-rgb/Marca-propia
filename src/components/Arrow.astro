---
interface Props { class?: string; diagonal?: boolean; }
const { class: className = '', diagonal = true } = Astro.props;
---
<svg class={className} width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><path d={diagonal ? 'M6 18 18 6M6 6h12v12' : 'M4 12h16m-7-7 7 7-7 7'} /></svg>
